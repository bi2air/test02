# Week of 2026-04-03 [BN2] — DRAFT (tier C — local-only, no closure metric)

> **Status:** weekly tier-1 page, draft. **Local-only — no Confluence push planned.** Sustained classifier R&D week without a single closing metric; the hypothesis line lands in the Apr 6+ infrastructure ship and the Apr 9-13 cache-experiment closure. Worth keeping in tier 2 for continuity but reads as in-progress noise to a reviewer.

**Week summary.** Continuous classifier R&D after the Sat Mar 28 `v1 get group code working` ship. Mon → Wed: three named experiment iterations (exp03 two-stage rerank → exp04 stage-code workflow → exp05c + `groupcode → stage code` rename) progressively productionize the two-stage classifier. Thu: `--whynot` argument adds explainability — when picking code X, also explain *why not Y* for runner-up candidates (direct support for the AGI-8632 audit). Fri: 5 Affirm classifier runs + 5 team-review batches consolidate outputs for cross-team review. The week ends without a closure metric — the hypothesis line lands the following week (Apr 6 postcall v1 + Apr 9-13 cache-experiment closure).

## Tickets this week

| Ticket | Status | Headline this week |
|---|---|---|
| [AGI-8632](https://trustingsocial1.atlassian.net/browse/AGI-8632) — [C09] Call disposition codes audited & redial logic tested | 🔵 In Progress | Five days of classifier R&D continuity: exp03 (Mon) → exp04 (Tue) → exp05c rename (Wed) → `--whynot` argument (Thu) → Affirm classifier 5-iteration day (Fri). |

(No DCI ticket this week — Tsel-side work picks up Apr 7+; MBF MHTT bundle arrives Apr 7.)

---

## 🛠 Headlines

> ### What I shipped this week
> - **exp03 two-stage LLM rerank (Mon Mar 30, 6 commits, 21 zero output dirs).** Call-code classifier moves from single-stage to two-stage: stage 1 emits a stage code (group-level), stage 2 picks the specific code, with rerank-from-stage-1 candidates. `b7241bb exp03 running two stages LLM, get match from rerank and natively` is the ship commit. Builds directly on the Sat Mar 28 weekend Appendix (`v1 get group code working`).
> - **exp04 stage-code workflow productionized (Tue Mar 31, 2 commits, 10 zero output dirs).** `938553b exp04, including some update in workflow to generate stage code` — captures the procedure for generating stage codes (the artifact stage 1 emits, stage 2 consumes) so the run is reproducible. `docs/workflow/stage_definition_run_20260331.md` is the dated workflow doc.
> - **exp05c + `groupcode → stage code` rename (Wed Apr 1, 2 commits).** `8eca70f exp05c, rename groupcode --> stage code (partly), update gitignore` — phasing out the legacy `groupcode` term across the codebase; canonical name is now "stage code". The `c` variant in exp05c carries forward the two-stage rerank line with a tweaked configuration.
> - **`--whynot` argument added (Thu Apr 2, 2 commits + 6 zero output dirs).** `d83e35e add whynot argument` — explainability for the call-code classifier: when picking code X, also output *why not Y* for the runner-up candidates. Output to `output/whynot/` and `output/datadog_whynot/`. Direct support for the AGI-8632 audit (debugging mis-classifications, compliance review).
> - **5 Affirm classifier iterations + 5 team-review batches (Fri Apr 3, zero: 15 dirs).** Five `affirm_20260403_*/per_call/` runs at 09:20 / 11:15 / 11:44 / 14:57 / 15:14 — iterative classifier-tuning loop with prompt or postprocess-condition tweaks between runs. Late afternoon: 5 `team-review/20260403_*` batches at 16:27 / 17:09 / 17:14 / 17:16 / 17:17 stage classifier outputs for cross-team labelling.

## Per-day timeline (links to tier 2)

| Day | Headline | Detail |
|---|---|---|
| **Sat Mar 28** | `40fab2f v1 get group code working` — the v1 ship enabled by Mar 27 Fri triple-run. | (→ Mar 30 Mon Appendix) |
| **Mon Mar 30** | **exp03 two-stage LLM rerank** (6 commits, 21 zero output dirs). | `worklogs/daily/2026-03-30.md` |
| **Tue Mar 31** | **exp04 stage-code workflow** (2 commits, 10 zero output dirs) + `docs/workflow/stage_definition_run_20260331.md`. | `worklogs/daily/2026-03-31.md` |
| **Wed Apr 1** | **exp05c + `groupcode → stage code` rename** (2 commits, 2 zero output dirs). | `worklogs/daily/2026-04-01.md` |
| **Thu Apr 2** | **`--whynot` argument** added (2 commits, 6 zero output dirs) — explainability for the call-code classifier. Output dirs: `output/whynot/`, `output/datadog_whynot/`. | `worklogs/daily/2026-04-02.md` |
| **Fri Apr 3** | **5 Affirm classifier runs** (09:20 → 15:14) + **5 team-review batches** (16:27 → 17:17 in 50 minutes). Zero: 15 dirs. | `worklogs/daily/2026-04-03.md` |

## Collaborators

- **Cross-team labelling consumers** (Fri team-review batches): the 5-batch handoff stages classifier outputs for adjudication — prepared by you, consumed by reviewers.

## Tools used

- **Claude Code** — exp01 → exp03 → exp04 → exp05c progression (rapid iteration loop on the two-stage classifier); `--whynot` argument scaffolding; 5-iteration Friday tuning + team-review batch packaging.

## Tier 2 reference

Per-day detail in `worklogs/daily/2026-03-{30,31}.md` and `worklogs/daily/2026-04-{01,02,03}.md`. **Local-only — no Confluence push** per the tier C rule (sustained R&D, no closing metric this week; the closure happens in the Apr 9-13 cache-experiment line).
