# Wednesday, 18 Mar 2026 (Binh Thanh Nguyen (2))

## Tickets

| # | Ticket | Status | Headline |
|---|--------|--------|----------|
| 1 | [DCI-328](https://trustingsocial1.atlassian.net/browse/DCI-328) — [MBF] Data schema check up [pre-contract] | ✅ Done (Apr 22) | Censored MBF sample bundle received (28 telco CSVs); first TS-side schema CSV drafted to drive next day's audit pipeline |

---

## Censored MBF sample arrival + TS_Schema first draft

**Why.** **Round 1 fulfilment** under [DCI-328](https://trustingsocial1.atlassian.net/browse/DCI-328): MBF responded to Mon's data-type request with a censored sample of the ~150-type universe. Mon's reverse-mapping work answered "which `tc_*` columns are MBF-covered" *symbolically* — driven by the Coordination Log's enriched mapping. The next gate is whether the MBF-delivered CSV files actually match the schema we'd inferred: real column names, real data types, fill ratios, table coverage. The censored sample is the first concrete dataset we can audit. Before today we were reasoning about a spreadsheet; after, we can audit real CSVs.

> ### 🛠 What I did
>
> - **Received the censored MBF sample bundle** — 28 CSVs covering the telco-domain families the bid scopes against: CDR / billing / VAS / promo / device-info / cell-info. The `_20260314_censored` suffix encodes both as-of date (14 Mar) and redaction state. Files: `action_audit`, `adc`, `cell_info`, `change_info`, `dttt_tbts`, `dttt_tbtt`, `dump_vas`, `ggsn`, `icc_gprs`, `icc_other`, `ims_call`, `main_balance`, `mnp_isdn`, `monthly_arpu`, `msc_dv`, `msc_ic`, `msc_og`, `msc_smo`, `msc_smt`, `payment`, `promo_balance`, `psc_daily`, `scratch`, `smpp_smo`, `smsc_tp_daily`, `sub`, `vasp`, `vlr_daily`, `voice_sms_charge`.
> - **Drafted the first `mbf_data_2026 - TS_Schema.csv`** — the TS-side schema source-of-truth that drives the next day's audit pipeline (read by playbook v1 to extract `required = {filename: [columns]}` and `required_samples = {(filename, col): sample_value}` per ETL file).

**Result.** Two artifacts staged for Thu's pipeline build: the censored MBF sample bundle (audit input), and the TS_Schema CSV (mapping input). Sample shape now visible: 28 files, mixed Vietnamese-and-English casing on column names, values redacted but column structure intact.

**Credits.** MBF data team — delivered the censored bundle (single bulk hand-off).

**Links.**
- `mbf/fintech_sample_data_20260314_censored/` — 28-file censored sample bundle. Input to Thu's audit pipeline.
- `mbf/outputs/mbf_data_2026 - TS_Schema.csv` — first version of the TS-side schema CSV; replaced Fri by an extended-coverage version.

---

**Tools.** None — receive-and-stage day; no scripting in the workspace.
