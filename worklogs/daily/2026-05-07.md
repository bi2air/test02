# Thursday, 07 May 2026 (Binh Thanh Nguyen (2))

**Generated:** 2026-05-07 22:53 +0700 (UTC 2026-05-07T15:53:13Z) · **Mode:** brief (OKR-anchored)

> **OKR scope.** Work today serves **O2-KR2** *(Auto QC for call codes. Reduce bug reports by human QC by ≥75%)* and **O4-KR1** *(Automated pipeline for data collection, annotation, prompt optimization and evaluation for call codes)* under the [Hai group OKR 2026Q2](https://trustingsocial1.atlassian.net/wiki/spaces/LR/pages/5142773771). Not tracked as JIRA comments today per direction.

## What the OKRs target

**O2-KR2** wants the volume of human QC bug-reports for call-code outcomes to drop by ≥75% — i.e. fewer escalations to an analyst, faster ticket closures. **O4-KR1** wants an end-to-end pipeline so analysts don't hand-stitch commands when investigating a call: lookup, evidence, annotation, evaluation should all be one chat-shaped workflow. Both share the same friction: today, when a call code looks suspicious, the loop is *SSH → run `postcall_qa_v1` → grep JSON → write up the why*. Compressing that loop is the daily progress measure.

## What we did

Built the evidence-extraction layer of PACO (the postcall agent bot). Phase 2A2 work shipped four typed `MissionContract`s in the brain registry — `postcall_qa` (verdict + decision_path), `pull_conversation` (transcript extract), `whynot` (desired-vs-actual diagnosis), and the new **`extract_evidence`** (call-code-aligned transcript portion with verbatim user-quote markers). All four share a 4-tier cache resolver: `postcall_analysis.json` → `<cid>.postcall_qa_v1.json` → `all.postcall.json` → live Datadog pull (`@conversation_id:<cid> @app:postcall_analysis`). The LLM rewriter now enforces forensic quoting — user/customer turns must be quoted **verbatim** in double quotes (the "fingerprint" rule), agent turns may be paraphrased but named entities (call codes, customer names, company names) survive. Slot-based session memory (`brain/session.py`) makes "rerun it"-style anaphora resolve to the last call_id. Mock Slack UI got operator polish: ⌘+Enter send, optimistic chat, live `/info` event trace at 1 Hz, conda-`dev` self-bootstrap on launch. Engineering detail at `paco/docs/paco_phase2a_engineering_log_20260507.md`; integration scenarios captured at `paco/docs/additional_test2.md`.

## What we got so far

- **123 / 123 hermetic tests in 0.52 s** (started at 16, +107 today across the new contracts, session memory, rewriter, extractors, registry invariants).
- **4-test live confirmed** against `Yv7gbbcidQbcuQGieZPOMw` (final code WRN, real cached call) and 1-test against `gL6UsxpSgao7HB1sgHx4Nw` (uncached → live DD pull → final code PPA, 96 events / 66 turns in ~14 s end-to-end). Forensic quoting verified: late-call user follow-ups (e.g. *"Sorry. I don't know nobody by that name."*) are no longer dropped by the renderer cap, EN/VI mixed phrasings preserve user quotes in original English while wrapping prose in the user's language.
- **Toward O2-KR2**: a bug-report investigation that used to require `ssh + run pipeline + read JSON + write summary` now resolves with `@paco show me the evidence for <cid>` returning a Q&A in ~14 s — the 75% reduction target is the next thing to actually measure with a baseline cohort of QC tickets.
- **Toward O4-KR1**: 4-mission registry + 4-tier cache resolver covers the four canonical operator questions (postcall summary / why-this / why-not / show-evidence). Adding a new mission is now one self-contained `MissionContract` file plus a one-line registry entry — no router edits — which is the shape O4-KR1 needs to scale to "annotation, prompt optimization, evaluation" missions.
- **Open follow-ups**: (a) wire PACO output into the QC ticket flow so closures auto-count toward O2-KR2's 75% target, (b) optionally write back live-pulled events to `all.postcall.json` so subsequent queries on the same uncached cid are 4 ms cache hits instead of 5+ s DD round-trips, (c) add a `prompt_optimization` mission that proposes `call_code_descriptions` edits when whynot diagnoses repeat boundary failures.
