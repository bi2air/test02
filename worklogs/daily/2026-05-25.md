# Sunday, 25 May 2026 (Binh Thanh Nguyen (2))

**Generated:** 2026-05-27 (retroactive from session recap) · **Mode:** brief (OKR-anchored)

**OKR scope.** Work serves **O2-KR2** *(Auto QC for call codes. Reduce bug reports by human QC by ≥75%)* under the [Hai group OKR 2026Q2](https://trustingsocial1.atlassian.net/wiki/spaces/LR/pages/5142773771), and the Affirm call-disposition backfill line (AGI-10834 epic). Local analysis, no JIRA comment trail — brief mode.

**Glossary.** **IDV** = identity verification successful; **HUA** = hang up during authentication. In the active Affirm `main.yaml`, `HUA` can be a postprocess *fallback* from `IDV` when `negotiation_from_script` is absent from `list_current_state`. **Backfill** here = re-evaluate historical calls against the new/redacted pipeline, then compare before→after labels (agreement/stability, not accuracy).

## What the OKR targets

O2-KR2 wants automated QC to catch call-code regressions before a human does. A backfill re-run is exactly the kind of event that QC must be able to read: when 888 historical Affirm calls get re-scored, *which* code transitions are expected corrections vs regressions vs runtime-context artifacts? Today builds the distribution-diff tooling that answers that, and surfaces one dominant, suspicious transition that needs root-causing before the backfill can be trusted.

## What we did

Analyzed the Affirm before→after backfill on `data/Affirm_postcall_backfill_0805_2205_2026 - before_after_backfill.csv` (888 calls; `call_code` before vs `call_code_backfill_redact_0522` after) and built a **reusable `backfill-eval-dist` skill** (`~/.codex/skills/backfill-eval-dist/`) that emits dedup checks, per-code retention, confusion matrices (counts + row% + col%), group transition matrix, flagged risk-transitions, and a review packet. Output at `data/outputs/backfill_eval_dist_20260525/`. Wrote the next-session handoff at `docs/session_recap/2026-05-25-backfill-idv-hua-handoff.md` with 12 sample `IDV→HUA` conversation_ids and a 5-bucket triage framework (expected_hua / redaction_regression / state_context_missing / prompt_classifier_error / data_join_artifact).

## What we got so far

- **888 calls, 0 duplicates** on `conversation_id`, `call_sid`, and the compound key. Global agreement **189/888 = 21.3% unchanged**, 78.7% changed — but the weighted metric is dominated by one transition.
- **Dominant shift: `IDV → HUA` = 473 rows** — 67.7% of all changed rows, 53.3% of the whole CSV. Net distribution movement: `IDV` 602→91 (−511), `HUA` 10→494 (+484), `NNP` 23→75 (+52). Flagged `stage_shift|dominant_volume`.
- **Working interpretation:** too large/concentrated to be stochastic drift; treat `IDV→HUA` first as a stage/postprocess candidate (the `negotiation_from_script`-absent fallback), or redaction removing verification evidence — **not** a business outcome. Investigation must inspect runtime `list_current_state` + rendered prompt, not just final labels.
- **Toward O2-KR2:** the skill is the reusable diff engine a QC dashboard would call on every backfill; the IDV→HUA case is the first concrete "is this a regression?" question it surfaced.
- **Open follow-ups:** pull raw postcall evidence (transcript + `StartPostCallAnalysis` + `dict_variable` + `list_current_state` + rendered prompt) for 10–20 of the sampled `IDV→HUA` rows, bucket by the 5-way triage, then estimate proportions across the 473-row population. **Do not report the before/after comparison as accuracy** until ground truth is added.
