# Thursday, 21 May 2026 (Binh Thanh Nguyen (2))

**Generated:** 2026-05-23 07:35 +07 (UTC 2026-05-23 00:35Z) · **Mode:** brief (two-workstream, retroactive)

> **Scope.** Two parallel workstreams today. (1) **Affirm call-code boundary reframe** for AGI-1084x payment-disposition tickets — the actual deliverable under epic **[AGI-10834](https://trustingsocial1.atlassian.net/browse/AGI-10834)** *"[Affirm] Affirm Call Disposition Enhancement"*. (2) **Auto-QC pipeline prototype** — OKR-aligned infra build, no JIRA ticket, serves **O2/Binh-KR2** *("Auto QC for call codes. Reduce bug reports by human QC by >=75%.")* + **O4/Binh-KR1** *("Automated pipeline for data collection, annotation, prompt optimization and evaluation for call codes.")* under [003.02 DSCI - Hai group OKR 2026Q2](https://trustingsocial1.atlassian.net/wiki/spaces/LR/pages/5142773771). Local engineering all day, no JIRA comment trail — hence brief mode.

## What the OKRs / epic target

**AGI-10834** is the Affirm-facing epic tracking call-disposition correctness regressions; the five Highest-priority sub-tickets (AGI-10846/47/48/49/54) each pin one failure mode: PPIF/CPR hiding successful PAY/PIF, postprocess force-mapping verbal payments to FTP via the missing `regist_payment_signal` signal, PPA-vs-FDP ambiguity when both map to the Affirm export code HPP, FDP over-tagging on vague future intent, and a May 8–22 backfill ticket gated on the rules landing. Product wants the boundaries crisp enough that an Affirm reviewer can replay each call code definition without a tie-breaker conversation. The OKR layer is a different cut — it asks whether *any* call-code regression at all gets caught by automation before product files a bug. Today's two workstreams hit both layers: the boundary reframe shrinks the population of bugs at the source, and the auto-QC pipeline shrinks the time-to-detect for whatever regressions still slip through.

## What we did

**Workstream 1 — AGI-10834 boundary reframe (Codex session, recapped at `docs/session_recap/20260521_affirm-agi1084-callcode-reframe.md`).** Reviewed the five new Highest-priority Affirm payment-disposition tickets (AGI-10846: PPIF/CPR hiding PAY/PIF; AGI-10847: FTP-via-missing-`regist_payment_signal` override; AGI-10848: PPA→HPP remapping; AGI-10849: FDP over-broad; AGI-10854: May 8–22 backfill), cross-checked the Google Sheet `Call Codes` mapping tab and established the **HPP-ambiguity rule** — master `FDP` and master `PPA` both export to Affirm `HPP`, so any "remap to HPP" ticket must first be resolved as a master-code decision between FDP and PPA. Settled the new definitions: **FDP** requires concrete scheduling evidence (account/loan + amount + future date + payment method + customer confirmation); **PPA** is agreement-level only (customer accepts a proposed plan but payment is not yet locked); vague future intent ("I'll pay tomorrow", "I'll use the app") downgrades to **NNP**; verbal-only same-call payment success outranks PPIF/CPR. Left a dirty patch on `agi-sm-dsci-configs/scripts/en_collin_kompato_affirm/main.yaml` covering NNP/FTP/PPIF/PPA/FDP + postprocess priority — to be tightened and committed in the next session.

**Workstream 2 — Auto-QC prototype (Claude session, recapped at `docs/session_recap/20260523_postcall-auto-qc-prototype.md`).** Designed + scaffolded a daily Cloud Run Job that pulls yesterday-NY's `call_reports` rows from BigQuery (stratified per `(vendor_id, call_code, direction)`), reruns each conversation through the postcall classifier under three voters sharing one byte-identical prerendered prompt (V1 = production yaml's model resolved as-of the call date via `git log --first-parent --before` on `agi-sm-dsci-configs`, V2 = `gemini-2.5-pro`, V3 = `gpt-5`), and writes per-call agreement booleans to BQ. Eight modules under `src/postcall/auto_qc/` (~900 LOC), `deploy/auto_qc/` Docker bundle, local cycle wrapper, and a ~16KB end-to-end deploy README. Pilot scope = Affirm only (`vendor_id=16`). All locally-verifiable build steps green; cloud deploy is user-runnable from the README. (Plan + remaining build days extended into 2026-05-22; this worklog captures the 2026-05-21 portion: design + scaffold + first verifications.)

## What we got so far

- **AGI-10834 boundary work:** master-code decision rules drafted for FDP / PPA / NNP / FTP / PAY / PIF + an HPP-ambiguity gate that has to be applied before any "remap to HPP" wording; dirty `main.yaml` patch in `dev` branch holds the in-progress definitions. **Next gate is a validation ledger** (4 FDP false positives, FTP no-DTMF examples, PAY/PIF-hidden-by-PPIF examples) before the AGI-10854 May 8–22 backfill can run.
- **Auto-QC pipeline:** all five locally-verifiable build steps green — BQ-sampler SQL dry-run, csv-adapter round-trip on a real `data/autoQA/` row, yaml-asof resolver (SHA `17fe3930c7ae` for Affirm@2026-05-19 confirmed against `git log`), 3-voter parallel fan-out with prompt-SHA equality (`6f2083e15143adbd…` matched across v1/v2/v3), and resume cursor (2nd run skipped all cids in 0.14s vs 42.55s fresh).
- **Toward O2-Binh-KR2:** measurement infrastructure in place; the ≥75% human-QC-reduction target is the next thing to actually measure once the Cloud Run Job is deployed and has ~2 weeks of `postcall_qa_result` rows for a baseline.
- **Toward O4-Binh-KR1:** "data collection → evaluation" half lands; "annotation + prompt optimization" halves (routing disagreement rows into a re-annotation queue, prompt-tuning loop) are out of scope for v1.
- **Open follow-ups:** (1) commit the yaml dirty patch + build the AGI-1084x validation ledger (~2–3h); (2) run AGI-10854 May 8–22 backfill once rules are validated (~1–3h, gated on rerun pipeline availability); (3) execute auto-QC GCP setup + Cloud Run deploy per README (~30 min user-driven); (4) Looker dashboard over `postcall_qa_run_summary` after the first scheduled auto-QC run lands data (~1h).

**Credits (AGI-10834 boundary workstream).** Thanh Vo (DSCI) — filtered the candidate conversations the new FDP / PPA / FTP / PPIF / FDP definitions are being evaluated against, then paired with Tran Thieu on the **backfill rerun** — deploying the updated postcall pipeline (with the new boundary definitions baked in) to the prod env and re-evaluating historical calls against the new definitions. This is the operational half of **AGI-10854** *(rerun/backfill Affirm payment call dispositions for May 8–22)* and is the gating step before the AGI-1084x rule changes can be reported back to product. Tran Thieu — co-owner of that prod-side deploy + backfill rerun.
