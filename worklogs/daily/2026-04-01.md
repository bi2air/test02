# Wednesday, 01 Apr 2026 (Binh Thanh Nguyen (2)) — STUB

> **Status:** evidence collected, awaiting harvest from other folders.

## Tickets

| # | Ticket | Status | Headline |
|---|--------|--------|----------|
| 1 | [AGI-8632](https://trustingsocial1.atlassian.net/browse/AGI-8632) — [C09] Call disposition codes audited & redial logic tested | 🔵 In Progress | **exp05c + `groupcode → stage code` rename** — phasing out the legacy `groupcode` term across the call-disposition classifier; canonical name is now "stage code". Continues the two-stage classifier line from Mar 30-31. |

## Known evidence

**postcall git commits today (2):**
- `8eca70f` exp05c, rename groupcode --> stage code (partly), update gitignore
- (1 more)

## Stage-code rename + exp05c

**Why.** Continuing the stage-code line. Today's commits rename `groupcode` → `stage code` partly (the canonical term is now "stage code" — the groupcode legacy term is being phased out) and run exp05c which carries forward the two-stage rerank line.

> ### 🛠 What I did
> Renamed `groupcode → stage code` across the codebase (partial; full sweep tracked in later commits). Ran exp05c — {describe what the c-variant tests}. Updated `.gitignore` to exclude run artifacts.

**Result.** {exp05c metrics — fill from artifacts.}

**Links.**
- Postcall commit `8eca70f`.

---

**Tools.** Claude Code — {if applicable}

---

## Harvest TODO

- [ ] Find exp05c output artifacts (`output/` scan + run log).
- [ ] Cross-reference with the Mar 30 / Mar 31 / Apr 2 progression — what was the running hypothesis?
- [ ] Check other repos for related changes.
