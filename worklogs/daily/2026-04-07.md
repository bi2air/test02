# Tuesday, 07 Apr 2026 (Binh Thanh Nguyen (2))

## Tickets

| # | Ticket | Status | Headline |
|---|--------|--------|----------|
| 1 | [AGI-8632](https://trustingsocial1.atlassian.net/browse/AGI-8632) — [C09] Call disposition codes audited & redial logic tested | 🔵 In Progress | 3 morning iterations on the Affirm classifier (`per_conversation/` + `postcall_debug/` bundles), continuing the Apr 6 postcall v1 / affirm v00 baseline. Zero: 657 dirs. |
| 2 | [DCI-321](https://trustingsocial1.atlassian.net/browse/DCI-321) — Telco Score Maintenance | 🔵 In Progress | Tsel CSV4 minimum-weeks audit afternoon kickoff (`map.md`, `tks_csv2_3.drawio`, first-pass per-branch tables) |
| 3 | [DCI-328](https://trustingsocial1.atlassian.net/browse/DCI-328) — [MBF] Data schema check up [pre-contract] | ✅ Done | MBF MHTT sample bundle (`SAMPLE_MHTT-20260407T080725Z-3-001.zip`, 782 KB) lands; unpack deferred to Wed |

---

## Per-conversation iteration on the postcall classifier (3 runs, ~2h)

**Why.** Continuing the Apr 6 postcall v1 / affirm v00 baseline. Three back-to-back morning runs is the pattern of a rapid prompt or postprocess-condition iteration loop — full-testset re-run after each change rather than a single sampled smoke test. The `postcall_debug/` parallel directory captures per-call internal state (LLM call records, postprocess conditions, classifier intermediate output) that the regular `per_conversation/` doesn't expose, so a regression on any single call can be traced back to its inputs without re-running.

> ### 🛠 What I did
>
> - **Three back-to-back classifier runs** (09:11 → 10:07): each produces a `per_conversation/` bundle with classifier output + reasoning per conversation against the Affirm testset.
> - **`postcall_debug/` bundles per run** — internal-state captures (LLM call records, postprocess conditions, classifier intermediate output) for any-time replay of a single call.
> - **Full-testset coverage per run** — 657 sub-directories total across the 3 runs + debug bundles, consistent with re-running the entire Affirm testset on each iteration rather than a sampled subset.

**Result.** Three iterations land with full per-conversation + debug bundles per run, building on the Apr 6 v1 baseline (1189 dirs). The debug bundles let a follow-up reviewer trace any classifier behavior back to its inputs without a re-run; the `per_conversation/` outputs are the substrate for the Apr 8+ next-iteration cycle.

**Links.**
- Zero: `output/20260407_0911/per_conversation/`, `output/20260407_0925/per_conversation/`, `output/20260407_1007/per_conversation/` — three runs.
- Zero: `output/postcall_debug/20260407_*` — debug bundles per call.
- Apr 6 baseline: `output/20260406_*` (1189 dirs, postcall v1 + affirm v00 + prompt-shorten ship).

---

**Tools.** Claude Code — rapid edit + zero-run loop; postcall_debug bundles enable single-call replay without re-running the full testset.

---

## DCI-321 · Tsel CSV4 minimum-weeks audit (afternoon kickoff)

**Why.** Tsel telco scoring (CSV4 = Credit Scoring Version 4) has a stakeholder-supplied "minimum weeks of clean data + 4w buffer" estimate in `TKS feature _ idata and clean - ds_estimate.csv` that's not code-verified. Wrong number = weekly score job crashes mid-run with no upstream history. After the morning postcall iteration, pivoted to scoping the Tsel audit so the cloud-cutover sizing has a defensible number.

> ### 🛠 What I did
> Afternoon kickoff (12:34–14:06) at `tsel/csv_code/`:
> - **`map.md`** (12:34) — framed the problem: structure of csv2-prod / csv3-prod, where the orchestration starts (Airflow → Jupyter → python), what `idata` vs feature artifacts mean, why the handcrafted minimum-weeks numbers need verification.
> - **`tks_csv2_3.drawio`** (13:26, 155KB) — TKS CSV2/CSV3 architecture diagram for orienting the DAG walk.
> - **`csv4_scoring_clean_data_weeks_topdown.tsv`** (13:00) + **`clean_data_min_weeks_csv4.csv`** (13:30) + **`clean_data_min_weeks.tsv`** (13:40) — first-pass per-branch tables.
> - **`TKS feature _ idata and clean - ds_estimate.csv`** (14:06) — copied the stakeholder-supplied input into the audit folder as the under-test artifact.
>
> Plus a touch on `csv3-prod/lib_csv3/pipeline/activity_csv3/base.py` (12:36) while reading the producer for `feat_activity`.

**Result.** Audit scope set; full DAG walk happens Wed Apr 8 (`csv4_pipeline_dag_map.md` + `csv4_clean_data_audit_report.md`). Final headline numbers (33w cold-start / 28w warm) ship in the Apr 23 09:51 deliverable bundle.

**Links.**
- `tsel/csv_code/map.md` — problem framing.
- `tsel/csv_code/tks_csv2_3.drawio` — architecture diagram.
- `tsel/csv_code/clean_data_min_weeks_csv4.csv`, `clean_data_min_weeks.tsv`, `csv4_scoring_clean_data_weeks_topdown.tsv` — first-pass tables.
- DCI-321 — Telco Score Maintenance.

---

## DCI-328 · MBF MHTT sample bundle arrives

**Why.** MBF bid-prep had been auditing against the encrypted-fintech sample (Mar 20 arrival) and the legacy code in `mbf_cs_airflow_202208`. Today the **MHTT** (Mobifone's billing/CDR system) sample bundle drops — first delivery from the MBF side that's closer to production data shape and lets the pipeline-side audit get validated against real file structure rather than inferred from a sample.

> ### 🛠 What I did
>
> - **Received and parked `SAMPLE_MHTT-20260407T080725Z-3-001.zip`** (782 KB) at `mbf/schema/`. Filename suffix `_3-001` indicates a multi-part bundle (parts 1+2 already in the bid-prep handoff history; this is part 3-001).
> - Held off unpacking until tomorrow — afternoon was already spent on the DCI-321 Tsel kickoff, and the MHTT audit needs the morning slot for proper schema-walk against `consitution_v3.md`.

**Result.** Sample bundle staged for Wed's audit ship.

**Links.**
- `mbf/schema/SAMPLE_MHTT-20260407T080725Z-3-001.zip` — MHTT sample bundle as received.
