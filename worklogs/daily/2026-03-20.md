# Friday, 20 Mar 2026 (Binh Thanh Nguyen (2))

## Tickets

| # | Ticket | Status | Headline |
|---|--------|--------|----------|
| 1 | [DCI-328](https://trustingsocial1.atlassian.net/browse/DCI-328) — [MBF] Data schema check up [pre-contract] | ✅ Done (Apr 22) | Re-ran the schema-matching pipeline through three iterations as inputs evolved (TS schema update, encrypted sample arrival, DICTIONARY MBF_2.xlsx); schema-mapping coverage grew progressively across the day |

---

## Schema-matching pipeline iterations + DICTIONARY MBF_2 ingestion

**Why.** **Round 2 close-out** under [DCI-328](https://trustingsocial1.atlassian.net/browse/DCI-328). Thu shipped the v2 pipeline against the *censored* sample; today the inputs evolved twice: (a) the TS_Schema CSV got an updated version with extended column coverage, (b) the *encrypted* (full-data) MBF sample arrived to replace the censored bundle. Each input change is one re-run; the schema-mapping output growing across the day (16 KB Thu → ~29 KB Fri-late, **+80% combined**) is the headline signal that more TS columns now have a confident match. Plus the canonical **`DICTIONARY MBF_2.xlsx`** lands as a third input for the next iteration cycle.

> ### 🛠 What I did
>
> - **Run #2 (morning)** — re-ran the playbook v2 pipeline with an updated TS schema. Schema-mapping CSV grew **+41% rows** vs Thu's baseline; audit_report grew slightly; gap and unmapped stayed comparable. Output suffix tag: `_20260320`.
> - **Wrote `working_docs.md`** — a small workspace anchor pointing to the canonical [DICTIONARY MBF_2 Google Sheet](https://docs.google.com/spreadsheets/d/1Cu3owNoWnEcTX8NexIFZqIzpGKMrihyW). The Sheet was authoritative before the local xlsx arrived; the anchor file stops anyone re-deriving the link.
> - **Received the encrypted MBF sample bundle** — same 28 file names as Wed's censored bundle, ~2× the per-file size on average (`action_audit.csv` 69 KB → 134 KB; `adc.csv` 118 KB → 246 KB), confirming the censored→encrypted gain is "more rows + more populated columns" rather than new tables. Single bulk hand-off.
> - **Run #3 (late morning)** — same v2 pipeline against the encrypted sample. Schema-mapping CSV grew **+27% rows** vs Run #2 — bid-relevant signal that the encrypted sample carries values the censored version had blanked. Output suffix tag: `_20260320_<HHMMSS>` to disambiguate same-day iterations in the `outputs/` folder. (The `_HHMMSS` discipline is introduced today and continued in subsequent re-runs.)
> - **Re-pivoted the schema-mapping output** into the MBF spreadsheet header so the bid team can paste it directly into the shared Google Sheet without column reshuffle (`outputs/mbf_data_2026 - schema_mapping_20260320.csv`).
> - **Ingested `DICTIONARY MBF_2.xlsx` (8.4 MB)** — the canonical MBF-side data dictionary in xlsx form. Becomes the input that Mon's dictionary-extraction pipeline run consumes (`outputs/dictionary_extract_manifest_20260323.csv`).

**Result.** Three pipeline iterations in one day, each one matching an input change. Schema-mapping coverage compounded: **+41% from the updated TS schema, then +27% on top from the encrypted sample**. The encrypted-sample run closes out the [DCI-328](https://trustingsocial1.atlassian.net/browse/DCI-328) Round 2: review delivered, **no blockers**. Three concrete clarifications surfaced in the `gap_report` (later confirmed in the follow-up meeting recorded on the DCI-328 description):

1. **`sta_datetime` on `tc_accounts`** — currently the latest-of-change marker; need to check whether it tracks a *major* change (owner change, reactivate) or *minor* (data added/removed) before relying on it for cohort definitions.
2. **promo amount** — structural change in the MBF schema vs our `tc_*` reads; needs remap rather than a 1:1 column swap. Higher impact because promo amount weighs in the credit signal.
3. **VAS** (`dump_vas` / `vasp`) — MBF's new setup is a wide-table (~100 columns), mostly empty per row; need to comb the columns, parse what's used, and plug into our existing VAS log structure rather than ingest the raw wide-table.

DICTIONARY MBF_2.xlsx staged for Mon's full-day dictionary extraction work.

**Credits.** MBF data team — delivered the encrypted bundle and the DICTIONARY MBF_2 spreadsheet.

**Links.**
- `mbf/fintech_data_sample_encrypted_20260320/` — full-fidelity 28-file sample bundle. Replaces the censored bundle for all downstream work.
- `mbf/DICTIONARY MBF_2.xlsx` — canonical MBF data dictionary (8.4 MB). Consumed by Mon's dictionary-extraction pipeline.
- `mbf/working_docs.md` — workspace anchor to the Google Sheet version.
- `mbf/mbf_data_2026 - TS_Schema.csv` — updated TS schema source-of-truth.
- `mbf/outputs/*_20260320*.csv` — Run #2 + Run #3 output sets (8 files total, with `_HHMMSS` suffix on the late-morning run).
- `mbf/outputs/mbf_data_2026 - schema_mapping_20260320.csv` — re-pivoted schema-mapping for paste-into-Google-Sheet.

---

**Tools.** Claude Code — drove three pipeline iterations as inputs evolved, each one a single re-run with the new input wired in. The `_HHMMSS` suffix discipline introduced today lets multiple same-day runs co-exist in `outputs/` without overwrite.
