# Friday, 27 Mar 2026 (Binh Thanh Nguyen (2))

## Tickets

| # | Ticket | Status | Headline |
|---|--------|--------|----------|
| 1 | [AGI-8632](https://trustingsocial1.atlassian.net/browse/AGI-8632) — [C09] Call disposition codes audited & redial logic tested | 🔵 In Progress | Groupcode eval triple-run on `testset_clean_20260327.csv` — pre-v1 tuning that enables the Sat Mar 28 ship of `40fab2f v1 get group code working` |
| 2 | [DCI-328](https://trustingsocial1.atlassian.net/browse/DCI-328) — [MBF] Data schema check up [pre-contract] | ✅ Done | MBF schema-audit ships: `consitution_v3.md` (final task spec) + `worker6_v4` (production auditor with per-column trace + per-table rollup outputs) |

---

## Groupcode eval triple-run — pre-v1 tuning

**Why.** **Groupcode** = the multi-call-code grouping abstraction (compliance-class group, payment-class group, etc.). The classifier's first stage emits a group; the second stage picks the specific code. Today's eval triple-run surfaces what's working and what's not at the group level, with two re-runs as configuration / prompt variants are tuned ahead of Sat's v1 ship.

> ### 🛠 What I did
>
> - **Cleaned the testset** at `testset_clean_20260327.csv` — deduplication + labeling pass before evaluation.
> - **Three back-to-back eval runs** (base, 16:04, 16:31) against the same testset, each producing the three-artifact set: `result_groupcode_eval_*.csv` (per-row results), `result_groupcode_eval_group_accuracy_*.csv` (per-group accuracy decomposition), `result_groupcode_eval_metrics_*.json` (overall metrics).
> - **Per-group accuracy** kept alongside the overall metric so the eval reports which group is dragging the overall down rather than a single top-line number.

**Result.** Three iterations land; per-group accuracy + metrics JSON capture the lift trajectory. Sat Mar 28 commits `40fab2f v1 get group code working` — today's triple-run is the gating signal that enabled the ship.

**Links.**
- Zero: `output/zz_202603/20260327/result_groupcode_eval_*.csv` — three result CSVs.
- Zero: `output/zz_202603/20260327/result_groupcode_eval_group_accuracy_*.csv` — per-group accuracy CSVs.
- Zero: `output/zz_202603/20260327/result_groupcode_eval_metrics_*.json` — metric JSONs.
- Zero: `output/zz_202603/20260327/testset_clean_20260327.csv` — the cleaned testset used as input.
- Sat Mar 28 commit `40fab2f` — `v1 get group code working` (the ship enabled by today's eval).

---

**Tools.** Claude Code / Cursor IDE — iterative edit + zero-run loop across the three iterations.

---

## DCI-328 · MBF schema-audit ships (worker6_v4)

**Why.** Continuation of the Mar 24-26 MBF bid-prep work. Thu's bake-off + `worker6_v2`/`v2_nb` evening iterations were the bridge; today's morning lands the final auditor.

> ### 🛠 What I did
>
> - **Tightened the task spec to `consitution_v3.md`** — third revision after Thu's `consitution_v2.md`. v3 captures the last bake-off lessons (handling of `<prepend>` chains for ordering, treatment of CSV3-only `f_name` patterns, what to do when a notebook reads a `tc_*` table behind an `if RUN_WITH_AIRFLOW` guard).
> - **Snapshotted `worker6_v4_bk/`** before the v4 cut — keeps the pre-change `build_audit.py` + outputs alongside, in case the v4 surgery regresses.
> - **Shipped `worker6_v4`** — final auditor against `consitution_v3.md`. Outputs: **`audit_detailed.csv`** (per-column trace) + **`audit_summary.csv`** (table-level rollup) + `gap_analysis.md` + `report.md`. `worker6_v3/` was an empty intent-marker — went straight to v4.

**Result.** Worker6_v4 is the production auditor for the rest of the bid prep — same input contract (the four `csv*_dev/` trees) but emits a two-tier output (per-column + per-table) that the bid team can paste into the customer conversation. `consitution_v3.md` is the spec going forward; later MHTT audits (Apr 8) cite it.

**Links.**
- `mbf/schema/consitution_v3.md` — task-spec v3 (canonical going forward).
- `mbf/schema/worker6_v4/audit_detailed.csv` + `audit_summary.csv` + `gap_analysis.md` + `report.md` — final auditor outputs.
- `mbf/schema/worker6_v4_bk/` — pre-v4 backup snapshot.
