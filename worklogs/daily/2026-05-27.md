# Tuesday, 27 May 2026 (Binh Thanh Nguyen (2))

**Generated:** 2026-05-27 · **Mode:** brief (OKR-anchored, two workstreams)

**OKR scope.** Both workstreams serve **O2-KR2** *(Auto QC for call codes. Reduce bug reports by human QC by ≥75%)* and the Affirm payment-disposition accuracy line (KR1 / AGI-10834 epic) under the [Hai group OKR 2026Q2](https://trustingsocial1.atlassian.net/wiki/spaces/LR/pages/5142773771). Local engineering + analysis, no JIRA comment trail — brief mode.

## What the OKRs target

The Affirm payment-disposition classifier sits at ~79% against gold on the 87-conv payment testset. Two open questions block further progress: (1) can a *better/more-reasoning model* push past that ceiling, or is the limit elsewhere? and (2) does the production **transcript redaction** quietly destroy the payment facts the classifier needs, causing call-code shifts? Both feed KR2 (what QC must watch) and the accuracy line (what's actually fixable).

## What we did

**Workstream 1 — advanced-model comparison (recap: `docs/session_recap/20260527_affirm-payment-model-comparison.md`).** Replayed the exact r3 rendered prompts for all 87 convs through four advanced models on LLM Hub with `reasoning_effort=high` (`claude-opus-4-6`, `claude-sonnet-4-6`, `gemini-3.5-flash`, `gpt-5.4`), scored head-to-head vs `gemini-3-flash` (the production no-thinking model) on the full set and on turn-count cuts (n≥10, n≥20). Standalone replay (`experiments/split_train_may27/replay_thinking.py`) — no deployment-code change. **Workstream 2 — transcript-redaction payment-detail check (recap: `docs/session_recap/20260527_transcript-redaction-payment-detail.md`).** Reran 14 Affirm conversations across four transcript sources (CSV review, BigQuery before-redaction, BigQuery after-redaction, Datadog rendered prompt) with the same `main.yaml` on zero, joined outputs with per-source redaction metrics (placeholder / date / amount / phone-like counts + text hash). Checklist captured at `docs/resource/check_transcript_redact.md`.

## What we got so far

- **Model comparison (full 87):** `gemini-3-flash` (no thinking) **79.3%** > `claude-opus-4-6` 77.0% > `claude-sonnet-4-6` 75.9% > `gpt-5.4` 71.3% > `gemini-3.5-flash` 69.0%. Same ranking holds on n≥10 (78.3% vs 75.9%…) and n≥20. **No advanced model with high thinking beats the simple flash model** — confirming the ~79% ceiling is a *missing-input floor* (payment-result signal not rendered into the prompt), not a model-capability or conversation-length limit. **Decision: keep `gemini-3-flash`; do not route this classification to advanced/thinking models** (pure cost/latency for negative accuracy).
- **Redaction check:** aggregate accuracy vs gold stayed flat at **11/14 for every transcript source** — redaction usually preserves overall outcome/tone. But the real risk is **payment-detail loss** at case level (same-day vs future date, amount, card, arrangement). Clearest source-sensitive case: `FZ36obfBhhMKBTXImkyZ-g`; `-A63Y6VjYSWgLQjo9dziSg` had an identical before/Datadog hash with different outputs, so it's rerun/model instability, not a source effect.
- **Toward O2-KR2:** both results sharpen what QC must watch — the ceiling is an *input* problem (render the payment signal), and redaction QC should count **payment-evidence fields**, not just placeholder volume.
- **Open follow-ups:** (1) draft the payment-signal rendering AGI ticket (the only lever that moves the 79% ceiling); (2) add payment-entity extraction metrics to the redaction comparison and repeat source-sensitive reruns to separate redaction effects from LLM nondeterminism; (3) expand the redaction check beyond 14 calls for a population-level rate.
