# 2026-04-06
# today action
## labeling and improve postcall callcode for Affirm 
1. label by LLM
- we have a team review, which give a good understand of main LLM candidates are doing here; we need to have a reliable way to vote for which call code should be choose 
- immediate; get the reviewer to pick up to call code right away, we may want to review it label
- human review and check
2. Reduce the call code template 
- so far the major in plain llm call is dict variable --> the reduce this will only need to call those needed in in stages. This is easy to short conversation, in which the few stages are in, and we can get the def in those stage only.
- how much of this will be saved (we) 
- break down portion of template, which one is contributing a long status, and which one can be cut without satisfying the 
3. compliance: which call code in 
- which call code are redial that when predict wrong from do not call, do not contact, 

# ticket on redial callcode
- risk to redial of non-compliance
- how this will happen:
- customer request do not call because they are not customer --> predict wrong customer --> but retry anyway
- customer said CDS, and they do not have time as they are busy till 12 --> predict call back at 12 --> risk
+ so we need a data set, each for 10 sample, that will correctly saying intent that should not be cause (focus on compliance risk now, )
+ run call code and check if the they will be fall into redial group --> this seem to be 100% certain
# ticket on call code
- work with the data, develop a good strategy to generate high accuracy postcall code for the next best of action
- what do we have>
- a 66 conv + some simulated conv (no postcall code is availabe --> need to develop by hand)
- what Ds needed here:
-- need to have a human call code --> need to group them and see what are needed 
-- then to create different response to 